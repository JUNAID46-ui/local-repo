services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./output:/app/output
      - ./logs:/app/logs
      - ./data:/app/data
      - ./credentials:/app/credentials
    env_file:
      - .env
    restart: unless-stopped
