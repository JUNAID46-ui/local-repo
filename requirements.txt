# Core
torch>=2.1.0
torchvision>=0.16.0
numpy>=1.24.0
opencv-python>=4.8.0
Pillow>=10.0.0

# Web server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Detection (Grounding DINO)
groundingdino-py>=0.4.0

# Segmentation (SAM 2)
sam-2>=1.0

# Video inpainting (ProPainter)
# Install from source: pip install git+https://github.com/sczhou/ProPainter.git
# propainter

# Image inpainting (LaMa) - uses TorchScript weights, no extra package needed

# Video processing
# ffmpeg must be installed system-wide: apt install ffmpeg / brew install ffmpeg

# Optical flow (RAFT) - included in torchvision
